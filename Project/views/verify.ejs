<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Verification Form</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            display: flex;
            flex-direction: column;
            min-height: 100vh;
        }

        footer {
            margin-top: auto;
        }
        
        form {
            width: 50%;
            margin: auto;
            padding: 20px;
            border: 2px solid #ccc;
            border-radius: 10px;
            background-color: white;
            box-shadow: 0px 0px 10px rgba(0,0,0,0.1);
        }
        label {
            display: block;
            margin-bottom: 10px;
        }
        input[type="file"] {
            margin-bottom: 20px;
        }
        button {
            padding: 10px 20px;
            margin-top: 10px;
            border: none;
            border-radius: 5px;
            background-color: #f48f1c;
            color: white;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }
        button:hover {
            background-color: #a393eb ;
        }
        button:active {
            background-color: #a393eb;
        }
    </style>
</head>
<body>
    <%- include('partials/nav.ejs')%>
    <form id="verificationForm" action="/verify" method="post" enctype="multipart/form-data">
        <label for="personal_identification_card">Personal Identification Card:</label>
        <input type="file" id="personal_identification_card" name="personal_identification_card" accept="application/pdf" required><br>

        <label for="SCN">Social Security Number (SCN):</label>
        <input type="file" id="SCN" name="SCN" accept="application/pdf" required><br>

        <label for="Tax_identification_number">Tax Identification Number:</label>
        <input type="file" id="Tax_identification_number" name="Tax_identification_number" accept="application/pdf" required><br>

        <label for="photo_of_passport">Photo of Passport:</label>
        <input type="file" id="photo_of_passport" name="photo_of_passport" accept="application/pdf" required><br>

        <button type="submit">Submit</button>
        <button type="button" onclick="clearForm()">Clear All</button>
    </form>
    <%- include('partials/footer.ejs')%>
</body>
<script>
    function clearForm() {
        document.getElementById('verificationForm').reset();
    }
</script>
</html>
